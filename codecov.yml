# Coverage gates are per layer (flags). Do not chase overall %.
# Shells → Playwright smoke, not Vitest line coverage.
# See docs/standards/STANDARDS.md & docs/standards/TESTING.md § Coverage.

coverage:
  precision: 2
  round: down
  range: "60...90"
  status:
    # Overall / default project+patch: informational only (shells skew %).
    project:
      default:
        informational: true
      shared:
        target: "90%"
        threshold: "1%"
        flags:
          - shared
      server:
        target: "75%"
        threshold: "2%"
        flags:
          - server
      web-core:
        target: "85%"
        threshold: "2%"
        flags:
          - web-core
      web-ui:
        target: "50%"
        threshold: "5%"
        flags:
          - web-ui
      ui:
        target: "90%"
        threshold: "5%"
        flags:
          - ui
    patch:
      default:
        informational: true
      shared:
        target: "90%"
        threshold: "0%"
        flags:
          - shared
      server:
        target: "75%"
        threshold: "2%"
        flags:
          - server
      web-core:
        target: "85%"
        threshold: "2%"
        flags:
          - web-core
      web-ui:
        target: "50%"
        threshold: "5%"
        flags:
          - web-ui
      ui:
        target: "90%"
        threshold: "5%"
        flags:
          - ui

# CSS modules must not fail patch/project status.
ignore:
  - "**/*.module.css"

flags:
  shared:
    paths:
      - packages/shared/**
    carryforward: true
  server:
    paths:
      - apps/server/**
    carryforward: true
  web-core:
    paths:
      - apps/web/src/lib/**
      - apps/web/src/transport/**
    carryforward: true
  web-ui:
    paths:
      - apps/web/src/shells/**
    carryforward: true
  ui:
    paths:
      - packages/ui/**
    carryforward: true

comment:
  layout: "reach,flags,files"
  behavior: default
  require_changes: false
  require_base: false
  require_head: true
  hide_project_coverage: false
