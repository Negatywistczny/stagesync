name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Cancel previous runs of the same pipeline when a new commit is pushed
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  changes:
    name: path-filter
    runs-on: ubuntu-latest
    outputs:
      web: ${{ steps.filter.outputs.web }}
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Detect web src changes
        uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            web:
              - 'apps/web/src/**'
              - 'apps/web/e2e/**'
              - 'apps/web/package.json'
              - 'apps/web/playwright.config.*'

  build:
    name: lint-types-test-build
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.0.10

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Lint SS CSS (no ad-hoc HEX / fractional rem)
        run: pnpm lint:ss-css

      - name: Sync launcher UI vendor + desktop launcher tests
        run: |
          pnpm sync:launcher-ui
          pnpm --filter @stagesync/desktop test

      - name: Check types
        run: pnpm check-types

      - name: Test
        id: unit_test
        run: |
          set -o pipefail
          pnpm test 2>&1 | tee "${RUNNER_TEMP}/unit-tests.log"

      - name: Summarize unit test failures
        if: failure() && steps.unit_test.outcome == 'failure'
        run: |
          log="${RUNNER_TEMP}/unit-tests.log"
          if [ ! -f "$log" ]; then
            echo "No unit-tests.log captured"
            exit 0
          fi
          echo "::group::Failed Vitest summary"
          # FAIL lines + Vitest mock/export errors + turbo package failure
          grep -E 'FAIL |Failed Tests|Error: \[vitest\]|No ".+" export is defined|Failed: ' "$log" \
            | sed 's/\x1b\[[0-9;]*m//g' \
            | head -n 100 \
            || true
          echo "::endgroup::"
          # Surface first distinct missing-export mock gaps as annotations
          grep -oE 'No "[^"]+" export is defined on the "[^"]+" mock' "$log" \
            | sed 's/\x1b\[[0-9;]*m//g' \
            | sort -u \
            | while IFS= read -r line; do
                echo "::error title=Incomplete Vitest mock::${line}"
              done \
            || true

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: |
            apps/web/test-results/junit.xml
            apps/server/test-results/junit.xml
            packages/shared/test-results/junit.xml
            packages/ui/test-results/junit.xml
          fail_ci_if_error: false

      - name: Coverage (shared + server + web + ui)
        run: pnpm test:coverage

      # Separate uploads so Codecov flags map 1:1 to packages (see codecov.yml).
      - name: Upload coverage (shared)
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true
          disable_search: true
          files: packages/shared/coverage/lcov.info
          flags: shared
          name: shared

      - name: Upload coverage (server)
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true
          disable_search: true
          files: apps/server/coverage/lcov.info
          flags: server
          name: server

      - name: Upload coverage (web)
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true
          disable_search: true
          files: apps/web/coverage/lcov.info
          flags: web-core,web-ui
          name: web

      - name: Upload coverage (ui)
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true
          disable_search: true
          files: packages/ui/coverage/lcov.info
          flags: ui
          name: ui

      - name: Build
        run: pnpm build

  # Forma Timeline smoke (path-filtered). See apps/web/e2e/README.md.
  playwright:
    name: playwright-smoke
    needs: changes
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'pull_request' && needs.changes.outputs.web == 'true')
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6.0.10

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared (server import)
        run: pnpm --filter @stagesync/shared build

      - name: Get Playwright version
        id: pw-version
        working-directory: apps/web
        run: echo "version=$(pnpm exec playwright --version | awk '{print $NF}')" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        id: pw-cache
        uses: actions/cache@v6
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.pw-version.outputs.version }}-chromium

      - name: Install Playwright Chromium
        if: steps.pw-cache.outputs.cache-hit != 'true'
        working-directory: apps/web
        run: pnpm exec playwright install --with-deps chromium

      - name: Install Playwright OS deps (cache hit)
        if: steps.pw-cache.outputs.cache-hit == 'true'
        working-directory: apps/web
        run: pnpm exec playwright install-deps chromium

      - name: Playwright Forma smoke
        working-directory: apps/web
        env:
          CI: true
          STAGESYNC_DATA_DIR: ${{ runner.temp }}/ss-e2e-data
        run: pnpm exec playwright test

  docker:
    name: compose-build
    if: github.event_name == 'workflow_dispatch' || (github.event_name == 'push' && github.ref == 'refs/heads/main')
    runs-on: ubuntu-latest
    timeout-minutes: 25
    needs: build

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Compose build
        run: docker compose build

      - name: Compose health smoke
        run: |
          # Image runs as node (uid 1000); bind-mounted ./data from the runner must be writable.
          mkdir -p data/library data/projects data/logs
          chmod -R a+rwX data
          docker compose up -d
          # Wait for healthcheck (up to ~2 min on slow runners)
          STATUS="missing"
          for i in $(seq 1 24); do
            sleep 5
            CID=$(docker compose ps -q stagesync 2>/dev/null || true)
            if [ -z "$CID" ]; then
              STATUS="missing"
            else
              STATUS=$(docker inspect --format='{{.State.Health.Status}}' "$CID" 2>/dev/null || echo "missing")
            fi
            echo "health attempt $i: $STATUS"
            if [ "$STATUS" = "healthy" ]; then
              break
            fi
            if [ "$STATUS" = "unhealthy" ]; then
              echo "container reported unhealthy"
              docker compose logs
              exit 1
            fi
          done
          if [ "$STATUS" != "healthy" ]; then
            echo "timed out waiting for healthy (last=$STATUS)"
            docker compose logs
            exit 1
          fi
          curl -sf http://localhost:4000/api/health || (docker compose logs && exit 1)
          docker compose down
