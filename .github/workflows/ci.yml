name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    name: lint-types-test-build
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello from stagesync ci"
